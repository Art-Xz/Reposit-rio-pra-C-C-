#include <stdio.h>

int main(){
    int w,x,y,z;
    int aw,ax,ay,az;
    int dia,hora,minuto,segundo;
    scanf("Dia %d",&w);
    scanf("%d : %d : %d",&x,&y,&z);
    scanf("Dia %d",&aw);
    int c;
    while ((c = getchar()) != '\n' && c != EOF);
    scanf("%d : %d : %d",&ax,&ay,&az);
    printf("%d %d %d",x,y,z);
    //inacabado:((()))
}